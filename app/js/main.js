$(function() {
  $('.first__slider').slick({
    arrows: false,
    dots: true,
    fade: true,
    autoplay: true,
    autoplaySpeed: 2000
  });

  $('.header-top__select').styler();
  $('.header-bottom__select').styler();
 

});